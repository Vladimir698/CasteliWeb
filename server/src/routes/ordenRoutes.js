'use strict';
const express=require('express');const c=require('../controllers/ordenController');const {requiereLogin,soloAdmin}=require('../middleware/authMiddleware');const router=express.Router();router.use(requiereLogin);router.get('/',c.listarActivas);router.get('/nueva/:vehiculoId',c.mostrarFormularioNuevo);router.post('/vehiculo/:vehiculoId',c.crear);router.post('/:id/trabajos',c.agregarTrabajo);router.post('/trabajos/:trabajoId/estado',c.cambiarEstadoTrabajo);router.post('/trabajos/:trabajoId/repuesto',c.agregarRepuestoDesdeTrabajo);router.post('/:id/repuestos/todos',soloAdmin,c.agregarTodosRepuestos);router.post('/:id/repuestos',soloAdmin,c.agregarRepuesto);router.post('/repuestos/:repuestoId/editar',soloAdmin,c.actualizarRepuesto);router.post('/:id/limpiar-trabajos',c.limpiarTrabajos);router.post('/:id/limpiar-cobro',soloAdmin,c.limpiarCobro);router.post('/:id/limpiar-detalle',soloAdmin,c.limpiarDetalle);router.post('/:id/totales',soloAdmin,c.actualizarTotales);router.post('/:id/finalizar',c.finalizar);router.get('/:id',c.verDetalle);module.exports=router;