'use strict';
const express=require('express');const c=require('../controllers/ordenController');const {requiereLogin}=require('../middleware/authMiddleware');const router=express.Router();router.use(requiereLogin);router.get('/',c.listarActivas);router.get('/nueva/:vehiculoId',c.mostrarFormularioNuevo);router.post('/vehiculo/:vehiculoId',c.crear);router.post('/:id/trabajos',c.agregarTrabajo);router.post('/trabajos/:trabajoId/estado',c.cambiarEstadoTrabajo);router.post('/trabajos/:trabajoId/repuesto',c.agregarRepuestoDesdeTrabajo);router.post('/:id/repuestos',c.agregarRepuesto);router.post('/repuestos/:repuestoId/editar',c.actualizarRepuesto);router.post('/:id/totales',c.actualizarTotales);router.get('/:id',c.verDetalle);module.exports=router;